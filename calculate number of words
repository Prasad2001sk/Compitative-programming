#include<stdio.h>
#include<string.h>
int main()
{
  char str[20];
  int i,c=0;
  printf("Enter string:");
  gets(str);
  for(i=0;str[i]!='\0';i++)
  {
      if(str[i]==' ')
      {
      c++;
      }
  }
  printf("\n Number Of Words=%d",c+1);
  return 0;
}
